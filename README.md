# Cecius

--------------------------------------------------TO START-------------------------------------------------------

1) Open power shell and cd to the project path
2)'npm init' (will create package.json file)
3)'npm install gulp -D' (will create package-lock.json file)
4)'npx -p touch nodetouch gulpfile.js' (will create gulpfile.js file - empty js file)
5)'gulp --help' optional - for instructions



--------------------------------------------------LIBRARIES-------------------------------------------------------
gulp
    gulp-uglifycss
    gulp-sass